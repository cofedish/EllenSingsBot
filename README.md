# 🎵 EllenSings

Музыкальный Discord-бот в стиле **Ellen Joe** из Zenless Zone Zero.
Чистый, стабильный, с поддержкой прокси и красивым UI.

---

## ✨ Возможности

- 🎧 **Воспроизведение музыки** из YouTube и других источников (через yt-dlp)
- 🎛️ **Интерактивное управление** через кнопки и slash-команды
- 📃 **Очередь с пагинацией** — удобный просмотр и управление треками
- 🔁 **Режимы повтора**: трек, очередь, без повтора
- 🌐 **Поддержка прокси** (HTTP/HTTPS/SOCKS5) для всего трафика
- 🎨 **Красивый UI** в стиле Ellen Joe — минималистичные embed'ы, аккуратные кнопки
- 🐳 **Docker** — запуск в один клик
- 🔒 **Стабильность** — исправлены race conditions, правильная обработка очереди

---

## 🚀 Быстрый старт

### 1. Предварительные требования

- **Docker** и **Docker Compose** (рекомендуется)
- Или **Python 3.11+** для локального запуска

### 2. Настройка

1. Клонируйте репозиторий:
   ```bash
   git clone <repo-url>
   cd EllenSingsV2
   ```

2. Создайте `.env` файл из примера:
   ```bash
   cp .env.example .env
   ```

3. Отредактируйте `.env` и добавьте токен бота:
   ```env
   DISCORD_TOKEN=ваш_токен_бота
   ```

   Получить токен: https://discord.com/developers/applications

### 3. Запуск через Docker (рекомендуется)

```bash
docker-compose up -d
```

Проверка логов:
```bash
docker-compose logs -f
```

Остановка:
```bash
docker-compose down
```

### 4. Запуск локально (без Docker)

Установите зависимости:
```bash
# Linux/macOS
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt

# Windows
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
```

Запустите бота:
```bash
python bot.py
```

---

## 🌐 Настройка прокси

Бот поддерживает прокси для **всего трафика** (Discord API, YouTube, и т.д.).

### Форматы

В `.env` добавьте переменную `PROXY_URL`:

```env
# HTTP/HTTPS прокси
PROXY_URL=http://proxy.example.com:8080

# SOCKS5 прокси
PROXY_URL=socks5://127.0.0.1:1080

# С авторизацией
PROXY_URL=http://username:password@proxy.example.com:8080
PROXY_URL=socks5://user:pass@127.0.0.1:1080
```

### Что проксируется?

- ✅ Discord API (WebSocket + REST)
- ✅ yt-dlp (YouTube и другие источники)
- ✅ Все HTTP(S) запросы бота

Если `PROXY_URL` не указан — бот работает без прокси.

### Docker Networking

По умолчанию бот использует **host network mode** в Docker (только Linux).

#### Linux (Ubuntu, Debian, etc.)

Контейнер запускается с `network_mode: host` — **контейнер в сети хоста**.

**Преимущества:**
- ✅ Можно использовать `127.0.0.1` для прокси на хосте
- ✅ Работает с локальными сервисами (signbox, nekoray, v2ray, clash)
- ✅ Не нужно пробрасывать порты

**Пример:**
```env
# Прокси на хосте слушает localhost:2080
PROXY_URL=socks5://127.0.0.1:2080
```

Популярные прокси-клиенты:
- **signbox** — обычно `:2080` или `:1080`
- **nekoray** — настраиваемый порт
- **v2ray** — обычно `:10808`
- **clash** — обычно `:7890`

#### Windows / macOS

Host mode **не поддерживается** на Windows/Mac. Используйте `host.docker.internal`:

1. Уберите `network_mode: host` из [compose.yml](compose.yml)
2. Измените `.env`:
   ```env
   PROXY_URL=socks5://host.docker.internal:2080
   ```

#### Альтернатива: Bridge mode на Linux

Если не хотите использовать host mode:

1. Уберите `network_mode: host` из [compose.yml](compose.yml)
2. Используйте IP хоста:
   ```env
   # IP шлюза Docker (обычно 172.17.0.1)
   PROXY_URL=socks5://172.17.0.1:2080

   # Или IP интерфейса хоста
   PROXY_URL=socks5://192.168.1.100:2080
   ```

---

## 📋 Команды бота

Все команды доступны как **slash-команды** (`/play`) и через префикс (`!play`).

### Основные команды

| Команда | Описание |
|---------|----------|
| `/play <запрос>` | Добавить трек в очередь (URL или поисковый запрос) |
| `/skip` | Пропустить текущий трек |
| `/stop` | Остановить воспроизведение и очистить очередь |
| `/pause` | Приостановить воспроизведение |
| `/resume` | Возобновить воспроизведение |
| `/queue` | Показать очередь треков (с пагинацией) |
| `/nowplaying` | Показать текущий трек с панелью управления |
| `/clear` | Очистить очередь (не останавливая текущий трек) |
| `/repeat [mode]` | Режим повтора: `none`, `track`, `queue` |
| `/ping` | Проверить задержку бота |

### Панель управления

Команда `/nowplaying` отобразит интерактивную панель с кнопками:

- ⏯️ **Пауза/Возобновление**
- ⏭️ **Пропустить трек**
- ⏹️ **Остановить**
- 🔁 **Переключить режим повтора**
- 📃 **Показать очередь**

Кнопки работают для всех участников канала.

---

## 🛠️ Архитектура проекта

```
EllenSingsV2/
├── bot.py              # Главный файл бота, загрузка cog'ов, поддержка прокси
├── cogs/
│   └── music.py        # Музыкальный модуль (команды, очередь, UI)
├── utils/
│   └── ytdl.py         # Обёртка для yt-dlp с поддержкой прокси
├── sounds/             # Папка для локальных звуков (опционально)
├── Dockerfile          # Docker образ
├── compose.yml         # Docker Compose конфигурация
├── requirements.txt    # Python зависимости
├── .env.example        # Пример конфигурации
└── README.md           # Документация
```

### Ключевые улучшения

1. **Стабильная очередь**:
   - Единая точка обработки (`process_queue`) с `asyncio.Lock`
   - Нет race conditions при добавлении треков
   - Корректная обработка окончания треков

2. **Поддержка прокси**:
   - Гибкая конфигурация через ENV
   - HTTP/HTTPS/SOCKS5
   - Для Discord API + yt-dlp

3. **UI в стиле Ellen Joe**:
   - Минималистичные embed'ы (цвет `#5BCEFA`)
   - Интерактивные кнопки управления
   - Пагинация очереди
   - Красивые сообщения об ошибках

4. **Docker**:
   - Healthcheck
   - Непривилегированный пользователь
   - Минимальный образ
   - Rotation логов

---

## 🔧 Разработка

### Структура Cog'ов

Бот использует **discord.py Cogs** для модульности:

- `cogs/music.py` — вся музыкальная логика
- Легко добавить новые модули через `bot.initial_extensions`

### Логирование

Логи с уровнем `INFO` и выше:
- Загрузка модулей
- Подключение к Discord
- Воспроизведение треков
- Ошибки

Формат: `YYYY-MM-DD HH:MM:SS | module | LEVEL | message`

### Перезагрузка модулей

Команда `!reload` (только для владельца бота) — перезагружает все cog'и без перезапуска.

---

## 🐛 Известные проблемы и решения

### Бот не подключается к голосовому каналу

- Проверьте, что установлен **ffmpeg**: `docker exec -it ellensings-bot ffmpeg -version`
- Убедитесь, что у бота есть права на подключение к каналу

### Ошибки при загрузке треков

- **"Video unavailable"** — видео удалено или недоступно
- **"Sign in required"** — требуется вход (недоступно для yt-dlp)
- **Region restricted** — используйте прокси

### Прокси не работает

- Для SOCKS5 убедитесь, что установлен `aiohttp-socks` (должен быть в `requirements.txt`)
- Проверьте формат `PROXY_URL` в `.env`
- Проверьте логи: `docker-compose logs | grep proxy`

---

## 📝 Лицензия

MIT License — используйте свободно!

---

## 🎨 Стиль Ellen Joe

Бот следует визуальному языку персонажа **Ellen Joe** из Zenless Zone Zero:

- **Цветовая палитра**: мягкие голубые/бирюзовые оттенки (`#5BCEFA`, `#98D8C8`, `#95E1D3`)
- **Минимализм**: чистые embed'ы без перегрузки информацией
- **Функциональность**: каждый элемент UI имеет чёткую цель
- **Аккуратность**: никаких аниме-штампов, строгий "служебный терминал"

---

## 📞 Поддержка

При возникновении проблем:

1. Проверьте логи: `docker-compose logs -f`
2. Убедитесь, что `.env` настроен правильно
3. Проверьте версии зависимостей: `pip list`
4. Создайте issue с описанием проблемы и логами

---
