# 📝 Changelog — EllenSings v2.0

## 🎯 Основные улучшения

### ✅ Исправлены критические баги

**Проблема**: В `cogs/music.py` были дублирующиеся методы и race conditions
- ❌ Метод `check_inactivity` определён дважды (строки 69 и 146)
- ❌ Метод `process_queue` определён дважды с РАЗНОЙ логикой (97-120 и 159-189)
- ❌ Race conditions при одновременном добавлении треков
- ❌ Странный код: случайный звук из `sounds/` играл ПЕРЕД каждым треком

**Решение**:
- ✅ Единая точка обработки очереди `process_queue()` с `asyncio.Lock`
- ✅ Правильная обработка окончания трека через `handle_track_end()`
- ✅ Убраны все дубликаты методов
- ✅ Убран код случайных звуков (мешал работе)

---

### 🌐 Поддержка прокси через ENV

**Что добавлено**:
- HTTP/HTTPS прокси для Discord API и yt-dlp
- SOCKS5 прокси (через `aiohttp-socks`)
- Поддержка авторизации (username:password)
- Полностью опционально — без `PROXY_URL` работает как раньше

**Файлы**:
- `bot.py` — connector с прокси для discord.py
- `utils/ytdl.py` — прокси для yt-dlp
- `requirements.txt` — добавлен `aiohttp-socks>=0.8.0`

**Использование**:
```env
PROXY_URL=socks5://127.0.0.1:1080
```

---

### 🎨 UI в стиле Ellen Joe

**Что улучшено**:

1. **Now Playing панель** (`/nowplaying`):
   - Embed с информацией о текущем треке
   - 5 интерактивных кнопок: ⏯️ ⏭️ ⏹️ 🔁 📃
   - Автоматическое обновление при изменении состояния

2. **Очередь с пагинацией** (`/queue`):
   - Класс `QueuePaginator` с кнопками ◀️ ▶️
   - 10 треков на страницу
   - Показывает текущий трек + список следующих

3. **Все команды — красивые embed'ы**:
   - Единая цветовая палитра (Ellen Joe style)
   - Мягкие голубые/бирюзовые оттенки: `#5BCEFA`, `#98D8C8`, `#95E1D3`
   - Красные для ошибок: `#FF6B6B`
   - Понятные сообщения об ошибках

4. **Новые команды**:
   - `/pause` и `/resume` — отдельно
   - `/clear` — очистить очередь без остановки
   - `/ping` — проверка задержки

---

### 🐳 Обновлён Docker

**Dockerfile**:
- ✅ HEALTHCHECK добавлен
- ✅ Непривилегированный пользователь `botuser`
- ✅ Оптимизация слоёв
- ✅ Убран `--no-install-recommends` где уместно

**compose.yml**:
- ✅ Убран `NET_ADMIN` (больше не нужен tun2socks)
- ✅ Добавлен `PROXY_URL` как опциональная переменная
- ✅ Rotation логов (max 3 файла по 10MB)
- ✅ HEALTHCHECK в compose

**Что убрано**:
- ❌ Зависимость от `tun2socks` (был хардкод `socks5://172.17.0.1:2080`)
- ❌ `NET_ADMIN` capability (теперь прокси через aiohttp)

---

### 📚 Документация

**Новые файлы**:
- `README.md` — полная документация (300+ строк)
- `QUICKSTART.md` — быстрый старт в 3 шага
- `.env.example` — пример конфигурации
- `.gitignore` — защита от утечки `.env`
- `CHANGELOG.md` — этот файл

**README включает**:
- Установку и запуск (Docker + локально)
- Настройку прокси с примерами
- Список всех команд
- Архитектуру проекта
- Troubleshooting
- Описание стиля Ellen Joe

---

## 📦 Структура проекта (после рефакторинга)

```
EllenSingsV2/
├── bot.py              # ✨ Обновлён: прокси, логирование, error handling
├── cogs/
│   ├── music.py        # ✨ Полностью переписан: очередь, UI, стабильность
│   └── events.py       # (пустой, для будущих расширений)
├── utils/
│   └── ytdl.py         # ✨ Обновлён: прокси, улучшенная обработка ошибок
├── sounds/             # Папка для локальных звуков
├── Dockerfile          # ✨ Обновлён: HEALTHCHECK, security
├── compose.yml         # ✨ Обновлён: прокси, логирование
├── requirements.txt    # ✨ Обновлён: aiohttp-socks
├── .env.example        # ✨ Новый
├── .gitignore          # ✨ Новый
├── README.md           # ✨ Новый
├── QUICKSTART.md       # ✨ Новый
└── CHANGELOG.md        # ✨ Новый (этот файл)
```

---

## 🔧 Технические детали

### Исправленные баги в music.py

| Баг | Исправление |
|-----|-------------|
| Дублирование `process_queue` | Единый метод с lock на строке 174 |
| Дублирование `check_inactivity` | Единый метод на строке 243 |
| Race conditions при добавлении треков | `asyncio.Lock` для каждой гильдии |
| Некорректный `after_play` callback | Разделены `after_play` (208) и `handle_track_end` (223) |
| Отсутствие обработки ошибок | Try-except блоки + логирование во всех командах |
| MusicControls не использовались | Интегрированы в `/nowplaying` и автообновление |

### Добавленные зависимости

```txt
aiohttp-socks>=0.8.0  # Для SOCKS5 прокси
```

### ENV переменные

| Переменная | Обязательная | Описание |
|------------|--------------|----------|
| `DISCORD_TOKEN` | ✅ Да | Токен бота с Discord Developers |
| `PROXY_URL` | ❌ Нет | Прокси (HTTP/HTTPS/SOCKS5) |

---

## 🎵 Новые команды

| Команда | Тип | Описание |
|---------|-----|----------|
| `/play` | Hybrid | Добавить трек (теперь с embed'ом) |
| `/skip` | Hybrid | Пропустить (улучшенные проверки) |
| `/stop` | Hybrid | Остановить + очистить |
| `/pause` | Hybrid | ✨ Новая: приостановить |
| `/resume` | Hybrid | ✨ Новая: возобновить |
| `/queue` | Hybrid | ✨ С пагинацией |
| `/nowplaying` | Hybrid | ✨ С интерактивной панелью |
| `/clear` | Hybrid | ✨ Новая: очистить очередь |
| `/repeat [mode]` | Hybrid | Режим повтора (улучшен) |
| `!ping` | Prefix | ✨ Новая: проверка задержки |
| `!reload` | Prefix | Перезагрузка модулей (только владелец) |

---

## 🚀 Что дальше?

### Возможные улучшения (не реализовано, но легко добавить):

- **Volume control** — регулировка громкости (slider или команда)
- **Плейлисты** — сохранение/загрузка плейлистов
- **Автоплей** — автоматическое добавление похожих треков
- **Статистика** — топ треков, история воспроизведения
- **Shuffle** — перемешивание очереди
- **Seek** — перемотка в треке (если yt-dlp поддерживает)
- **Эквалайзер** — фильтры ffmpeg (bass boost, nightcore, и т.д.)

---

## 📊 До и После

### До рефакторинга
- ❌ Race conditions при добавлении треков
- ❌ Дублирующиеся методы
- ❌ Хардкод прокси в start.sh
- ❌ NET_ADMIN в Docker
- ❌ Нет UI кнопок (были созданы, но не работали)
- ❌ Нет документации

### После рефакторинга
- ✅ Стабильная очередь с lock
- ✅ Чистый код без дублей
- ✅ Гибкий прокси через ENV
- ✅ Минимальные права в Docker
- ✅ Полноценный UI с кнопками
- ✅ Подробная документация

---

**Дата**: 2025-11-08
**Версия**: 2.0
**Статус**: Production Ready ✅
