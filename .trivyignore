# gateway/Dockerfile осознанно работает под root:
# контейнеру нужны NET_ADMIN, /dev/net/tun, iptables и ip route.
# Это единственный привилегированный контейнер; бот работает под
# непривилегированным пользователем с cap_drop: [ALL].
AVD-DS-0002
